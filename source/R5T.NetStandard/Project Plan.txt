R5T.NetStandard - A library for R5T.NetStandard functionality that depends only on base .NET Standard functionality and other libraries in the the R5T.NetStandard project namespace, but has not yet been broken off into its own sub-package.

Goals

